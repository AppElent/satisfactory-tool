// @ts-nocheck

import Network from '../calculator/network/network';
import Cytoscape from './cytoscape';

export default class Mermaid {
  constructor() {}

  fromNetwork = (network: Network): string => {
    return '';
  };

  fromCytoscape = (cy: Cytoscape): string => {
    return '';
  };
}
